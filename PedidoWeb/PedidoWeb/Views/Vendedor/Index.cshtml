@model IEnumerable<PedidoWeb.Models.Vendedor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PercDescontoMaximo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PercDescontoMaximo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VendedorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VendedorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VendedorID })
        </td>
    </tr>
}

</table>
